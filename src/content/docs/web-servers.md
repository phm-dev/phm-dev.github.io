---
title: Web Server Integration
description: Local development server and PHP-FPM configuration for web servers
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex
---

:::note[Work in progress]
This section is under development. For local development, we recommend using the [Symfony CLI](/tools/#symfony-cli) built-in web server — it requires zero configuration and works seamlessly with PHM.

```bash
phm install symfony
symfony server:start -d
```
:::
